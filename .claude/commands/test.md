---
description: Run the project's unit tests and report pass/fail
---

Run `node test/thaiDate.test.mjs` and report whether it passed.

If it fails, show the assertion error and point at the relevant logic in
`src/lib/thaiDate.js` (usually `parseQuickAdd`). Do not fix anything unless asked.
